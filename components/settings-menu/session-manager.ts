import { GObject, register, property, signal } from "astal"

export class LoginManager {
  public static Reboot(interactive: boolean) { }

  public static LockSessions() { }

  public static LockSession(id: number) { }
}
